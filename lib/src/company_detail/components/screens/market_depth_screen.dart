import 'package:flutter/material.dart';

class MarketDepthScreen extends StatelessWidget {
  const MarketDepthScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}