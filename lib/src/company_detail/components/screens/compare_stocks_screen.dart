import 'package:flutter/material.dart';

class CompareStocksScreen extends StatelessWidget {
  const CompareStocksScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
