import 'package:flutter/material.dart';

class RightShareScreen extends StatelessWidget {
  const RightShareScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}